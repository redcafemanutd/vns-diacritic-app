services:
  - type: web
    name: diacritic-webapp
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: OPENAI_API_KEY
        fromDotEnv: true
      - key: X_API_KEY
        fromDotEnv: true
